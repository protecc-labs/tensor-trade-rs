query UserTcompBids($owner: String!) {
  userTcompBids(owner: $owner) {
    bid {
      address
      amount
      field
      fieldId
      filledQuantity
      quantity
      solBalance
      target
      targetId
    }
  }
}
